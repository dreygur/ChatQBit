TELOXIDE_TOKEN=<Your Telegram Bot Token>
QBIT_HOST=<QBitTorrent Web UI address with PORT. Default: http://127.0.0.1:8080>
QBIT_USERNAME=<QBitTorrent Username. Default: admin>
QBIT_PASSWORD=<QBitTorrent Password>

# File Server Configuration (Optional - for streaming feature)
FILE_SERVER_HOST=0.0.0.0
FILE_SERVER_PORT=8081
FILE_SERVER_BASE_URL=http://localhost:8081
FILE_SERVER_SECRET=change_me_in_production

# Docker path mapping (Required when running in Docker with host qBittorrent)
# Set to the container mount point for qBittorrent downloads
# Example: If qBittorrent saves to D:\Torrents and you mount it to /downloads
# QBIT_DOWNLOAD_PATH=/downloads

# Tunnel Configuration (Optional - for remote access)
# Options: localhost.run, cloudflare, none
# localhost.run: Free, unlimited (uses ssh2 Rust library - no SSH binary required)
# cloudflare: Free, reliable (requires cloudflared binary)
# none: No tunnel, use FILE_SERVER_BASE_URL as-is
TUNNEL_PROVIDER=none

# Logging Configuration (Optional)
# Set to true to enable logging to file (in addition to console)
LOG_TO_FILE=false
# Path to log file (default: chatqbit.log in current directory)
# Can use subdirectories, e.g., logs/chatqbit.log
LOG_FILE_PATH=chatqbit.log
